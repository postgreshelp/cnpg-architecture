apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio-data
  namespace: minio
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 5Gi    # POC override for an 8GB-RAM laptop; TARS's real repo2 S3 capacity is 1TB
