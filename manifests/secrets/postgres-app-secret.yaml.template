# Filled in automatically by scripts/generate-secrets.sh (random password each run).
# App-level user CNPG creates during initdb, matching Cluster spec's bootstrap.initdb.owner.
apiVersion: v1
kind: Secret
metadata:
  name: tarsdb-app-user
  namespace: tars-db
type: kubernetes.io/basic-auth
stringData:
  username: tars_admin
  password: "REPLACED_BY_GENERATE_SECRETS_SH"
